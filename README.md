# The System

Runs everything.

## Requirements

* `python3`
* `pip`
* `virtualenv`

## Setup

1. Clone this repo
2. Clone the following repos within this repo:
	* `scanner`
	* `finder`
	* `classifier`
3. Run `setup.sh`

### Usage